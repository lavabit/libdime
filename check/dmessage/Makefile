checks_SOURCES		= checks.c check_dmsg.c
checks_CFLAGS		= $(LIBDMESSAGE_CFLAGS) $(LIBCORE_CFLAGS) $(LIBSIGNETRESOLVER_CFLAGS) $(LIBDIME_CFLAGS) $(LIBSIGNET_CFLAGS) $(LIBCOMMON_CFLAGS) $(DONNA_CFLAGS) $(OPENSSL_CFLAGS)
checks_LDADD		= $(LIBDMESSAGE_LIBS)   $(LIBCORE_LIBS)   $(LIBSIGNETRESOLVER_LIBS)   $(LIBDIME_LIBS)   $(LIBSIGNET_LIBS)   $(LIBCOMMON_LIBS)   $(DONNA_LIBS)   $(OPENSSL_LIBS)
CLEANFILES		= *.keys

include ../../check.mk
